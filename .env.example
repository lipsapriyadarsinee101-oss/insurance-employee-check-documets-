# Optional environment settings (the application does not auto-load this file).
# Set these in the shell before running the API/UI.
# CLAIMS_DB_PATH=local-data/reviews.sqlite3
# API_URL=http://127.0.0.1:8000
